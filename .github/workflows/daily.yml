name: Publicar actuación diaria 🇩🇴

on:
  schedule:
    - cron: '0 13 * * *'  # 13:00 UTC = 9:00 AM República Dominicana
  workflow_dispatch:

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ejecutar publicación
        run: |
          python -c "import fetch_stats, create_image, tweet_poster; df = fetch_stats.obtener_actuaciones(); img = create_image.generar_tabla(df); tweet_poster.publicar_en_twitter(img)"
        env:
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
